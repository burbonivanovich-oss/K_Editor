# Инструменты, скрипты и автоматизация

Справочник по тулчейну модуля. Что куда писать при изменении — в конце
документа («Документация изменений»).

## QA-инструментарий

### Фактчек — `/factcheck <slug>`

Свой стек проверки фактов (без плагина claude-blog, который в облачном
эфемерном контейнере не выживает между сессиями).

- **`scripts/factcheck/extract-claims.mjs`** извлекает claims
  (даты, штрафы, ст. КоАП/УК/НК, номера ПП/Приказов/ФЗ, ссылки).
- **`/factcheck <slug>`** сверяет каждый claim с первоисточником через
  WebSearch/WebFetch, опираясь на `src/data/factcheck/sources.json`.
- **`/factcheck-batch [--count N] [--filter <topic>]`** — пакетный прогон
  8–16 статей через параллельный subagent dispatch (×6 быстрее, чем по одной).
- **`scripts/factcheck/audit-npa-references.mjs`** — регрессионный аудит
  упоминаний НПА против `sources.json.npaWhitelist`. Флаг `--strict` для CI.
- **`scripts/factcheck/audit-marker-hashes.mjs`** — для каждой `draft:
  false` статьи в закоммиченном дереве сверяет хеш маркера с
  содержимым файла. Флаг `--strict` для CI — ловит правки после
  факчека, которые проскочили мимо pre-commit-хука (`--no-verify`).
- **Решения** принимаются по `docs/editorial-policy.md` — классы A/B/C.
- **Результат:** `src/data/factcheck/results/<slug>.json`
  (`{claims, summary}`) + маркер `.claude/factchecked/<slug>`
  (`{date, hash, result, criticalMismatches, rulesVersion, report}`).

### Шлюзы качества

**Мёртвая проверка — главный риск после перехода на скрипт.** Пока
пункт был строчкой чек-листа, его читали глазами — плохо, но читали.
Скрипт не читает никто, и проверка, которая всегда отвечает «зелёное»,
живёт вечно. Две такие нашлись сразу: каталог Маркета (разбор вывода не
тем шаблоном) и граф ссылок (linkgraph относит к сиротам только
выпущенные статьи, а гейт работает с черновиками). Защита —
`gates-liveness.test.mjs`: для каждой проверки есть вход, на котором она
обязана сказать «нет». Проверку без такого входа тест называет по имени.

**Пайплайн — четыре стадии** (сведён 13.08.2026 с девяти): ресёрч →
текст → гейты → оценка. Все детерминированные проверки собраны в одну
команду `npm run gates -- <slug>`; отдельных чек-листов по SEO, объёму,
перелинковке и каннибализации больше нет. Дубль блокирует только на
стадии 1, до ресёрча: находить его после написанной статьи значит терять
работу. Бриф на визуал остался обязательным артефактом, но перестал быть
шлюзом — качество текста от ТЗ на обложку не зависит.

**Оценка статьи разведена на две части** (переделано 13.08.2026). То, что
считает скрипт, баллов не приносит — это pass/fail и блокер. Баллы
0–100 ставятся только за лид, структуру, язык и пользу, то есть за то,
что видно при чтении. Оценивает отдельный прогон (`article-grader`), а
не та сессия, которая писала: до переделки автор оценивал себя сам, и
четыре статьи подряд получили 99–100 при живых нарушениях, записанных в
той же оценке. Порог допуска — 85. Подробности и разбор дыр старой шкалы
— в шапке `scripts/check-analysis.mjs`.

- **`scripts/release-article.mjs <slug>`** — единственный способ снять
  `draft` (в т.ч. из `/cycle-listen` — второго пути нет). Детерминированный
  CLI (не промпт): приёмка в `editorial-cycle.json`, все аудиты, свежесть
  `/analyze-article`, хеш и возраст факчек-маркера — всё в одном месте и
  в одном порядке. После успешного выпуска переводит строку темы в
  `content-plan-2026.md` на `done` и перегенерирует `editorial-plan.json`,
  если тема там была. Флаги: `--dry-run`, `--json`, `--confirm-no-cycle
  "<причина>"` (для статей вне цикла — причина обязательна, попадает в
  `analyze/<slug>.json.cycleReleaseOverride`), `--override-score
  "<причина>"` (принять балл `/analyze-article` ниже порога 85 — причина попадает в
  `analyze/<slug>.json.releaseOverride`; остальные гейты не снимает).
- **`/analyze-article <slug>`** — оценка 0–100 по 6 категориям. Блокер,
  если балл < 85, маркер факт-чека старше 180 дней, либо упали
  `factcheck/audit-npa-references.mjs` или `audit/check-blog-links.mjs`.
- **`scripts/check-ai-markers.mjs`** — AI-маркеры в тексте: шаблонные
  фразы, пассивные конструкции, структурные признаки. Порог — 6/10.
  Дополнительно считает (не входит в скор/порог — справочно, до
  калибровки на реальном корпусе статей): однородность ритма
  предложений, симметрию H2-секций по объёму, повтор словосочетаний
  (n-граммы). Для каждого структурного сигнала, вышедшего за эвристический
  порог, выводит конкретное редакторское действие (что перестроить и
  почему) — не только число. См. `docs/audit-2026-08-05-ai-likeness.md`.
  Флаг `--save-profile` (не по умолчанию — гейт в `release-article.mjs`
  его не включает) пишет срез оценки в `src/data/ai-profile/<slug>.json`
  с версией анализатора (`analyzerVersion`) и историей предыдущих
  прогонов (`history`) — чтобы сравнить статью «до» и «после» правки, а
  не только увидеть текущий скор.
- **`scripts/check-seo.mjs`** — P0 (блокирует): title/description
  заполнены и в пределах длины, категория и 4+ тега, `seo.keywords`,
  хотя бы одна внутренняя ссылка, FAQ-блок. P1 (предупреждение): ключ
  из `seo.keywords` в title, число H2, промоблоки не по кластеру.
- **`scripts/check-stale-content.mjs`** — статьи, где даты и сроки могли
  устареть. Основа для `/maintain-content`.
- **`scripts/maintain-content-queue.mjs`** — детерминированная сборка
  очереди `/maintain-content`: `maintain-queue.md` + reviewDate/маркер
  факчека + динамика спроса по Wordstat (recent3/baseline6). Раньше эта
  арифметика считалась в промпте вручную.
- **Pre-commit hook** (`scripts/git-hooks/`) для статьи с `draft: false`
  блокирует коммит без маркера `.claude/factchecked/<slug>` и при
  P0-ошибках `check-seo.mjs`. Установка: `bash scripts/git-hooks/install.sh`.

Факт-чек обязателен до передачи статьи редактору. Подробности —
`docs/factcheck.md`, `docs/factcheck-history.md`, `docs/editorial-policy.md`.

### Расширенный QA: плагин claude-blog (опционально)

[claude-blog](https://github.com/AgriciDaniel/claude-blog) — внешний плагин.
**В облачном контейнере не сохраняется между сессиями.** Если работаете
локально и хотите им пользоваться — установите вручную в
`~/.claude/skills/`. В облачных сессиях используем свой `/factcheck`.

Полезные команды плагина, если он установлен:

- `/blog analyze <file>` — оценка 0–100 по 6 категориям, детектирует AI-текст.
- `/blog seo-check <file>` — SEO-чеклист: title, meta, H2, внутренние ссылки.
- `/blog factcheck` — **не используем**, есть свой `/factcheck`.

## Wordstat: два контура

Используется в workflow `wordstat-weekly.yml` и `wordstat-kontur.yml`.

- **A. Точечный** — `src/data/wordstat/keys.json` хранит частоты и тренды
  по нашим `seo.keywords` и «Целевому запросу» из контент-плана. Читают
  `/find-topics`, `/cluster-gaps`, `/maintain-content`.
- **B. Discovery** — `discoveries/<date>/*.json` хранят топ-2000 фраз
  вокруг каждого из 162 широких seed-ов. `diff-snapshots.mjs` каждую
  неделю сравнивает с предыдущим прогоном и пишет в
  `discoveries/diffs/<date>.md` отчёт NEW/RISING/FALLING/DROPPED.
  `/find-topics` читает свежий diff как главный источник идей.

**Лимиты API:** 1000 запросов/сутки на токен, 10 req/sec. Бюджет weekly
прогона — ~640 квот. Подробности — `docs/wordstat.md`.

## Редакционный цикл

- **`scripts/cycle-state.mjs`** — машина состояний цикла. Единственный
  способ менять `src/data/editorial-cycle.json`; руками JSON не править.
- **`scripts/drive-sync.mjs`** — мост в Google Drive: создание папки цикла
  и таблицы плана, чтение решений редактора, выкладка статей доками,
  экспорт дока обратно в markdown, чтение, создание и закрытие
  комментариев. Позвать редактора письмом — `notify` (доступ к файлу с
  `sendNotificationEmail`); `comment --mention` письма не гарантирует и
  каналом уведомления не считается. Диагностика доступа — `check`.
- **`scripts/topics/segments.mjs`** — кластер → сегмент аудитории
  (розница, кассы и ОФД, общепит, услуги). Раскладка снята с ручного
  списка редакции, не придумана; спорные кластеры дают пусто.
- **`scripts/weekly-digest.mjs`** — недельная сводка редактору: что ждёт
  его решения и сколько дней ждёт. Выкладывается новым доком каждую
  неделю — только так Google шлёт письмо повторно.
- **`scripts/design-brief.mjs`** — исходные данные для ТЗ дизайнеру:
  структура статьи, разделы под схему, даты и суммы для врезок. Само ТЗ
  пишет `/design-brief` — это рассуждение, а не разбор.

Полное описание — `docs/editorial-cycle.md`, настройка доступа —
`docs/google-api-setup.md`.

## Карта скриптов (scripts/)

52 скрипта и 30 файлов тестов. Все на чистом Node без зависимостей,
запуск — `node scripts/<путь>.mjs`. Скрипты с сетевыми вызовами понимают
`DRY_RUN=1`. Полный список ниже: что делает и кто запускает. «Вручную» —
значит зовут только человек или агент по ситуации, в автоматике не стоит.

### Редакционный цикл

| Скрипт | Что делает | Кто запускает |
|---|---|---|
| `cycle-state.mjs` | Машина состояний: статусы тем, потолок очереди, разбор решений редактора. Единственное место, где меняется `editorial-cycle.json` | рутины, workflow, команды |
| `drive-sync.mjs` | Мост в Google: таблица, документы статей, комментарии, права доступа, точечная правка текста | рутины, workflow, команды |
| `verify-sheet.mjs` | Сверяет таблицу с состоянием цикла: раскладка колонок, статусы, ссылки, ID | workflow |
| `drive-sync.mjs sync-columns` | Приводит колонки живой вкладки к `WORK_COLS` вставкой и удалением столбцов. Сухой прогон по умолчанию | вручную, при смене набора колонок |
| `nudge-editors.mjs` | Напоминает редакции, когда очередь вычитки встала. Не чаще раза в сутки | workflow |
| `weekly-digest.mjs` | Недельная сводка редактору: что ждёт решения и сколько дней ждёт | рутина B |
| `editor-edits.mjs` | Журнал прямых правок: что бот написал и на что редакция это заменила | рутина B |
| `release-article.mjs` | Финальный шлюз выпуска: пять проверок подряд, снимает `draft` только если прошли все | рутина B, команда |
| `design-brief.mjs` | Детерминированная часть ТЗ дизайнеру: цифры, даты и сущности из статьи | команда |
| `social-state.mjs` | Память об адаптациях статьи под соцканалы | команда |
| `update-queue.mjs` | Очередь актуализации: пачка ссылок от редакции → темы цикла, со сведением с каталогом Маркета, снятием повторов и порядком по трафику | команда, рутина A0 |
| `lib/untrusted-text.mjs` | Граница «данные, не команды»: ищет в тексте из таблицы и комментариев обороты, адресованные процессу, и держит список доменов, которые автономная сессия грузит по ссылке | `cycle-state`, рутины |
| `lib/update-task.mjs` | Разбор ячейки «Тема» на задачу: новая статья или актуализация, ссылка на исходник, просьба редактора отдельно от заголовка | `cycle-state` |

### Отбор тем и спрос

| Скрипт | Что делает | Кто запускает |
|---|---|---|
| `topics/generate-backlog.mjs` | Собирает недельный список тем: дифф спроса, фильтры, квоты по полосам и кластерам, дедуп с корпусом и каталогом Маркета | workflow бэклога |
| `topics/suppressions.mjs` | Память об отказах редактора: что глушим, на сколько и по какой похожести | `generate-backlog`, `cycle-state` |
| `topics/priority-clusters.mjs` | Вес кластера при скоринге темы | `generate-backlog` |
| `topics/segments.mjs` | Кластер → сегмент аудитории для карточки темы | `generate-backlog` |
| `generate-editorial-plan.mjs` | Контент-план из markdown → `editorial-plan.json` | рутина A, workflow |
| `maintain-content-queue.mjs` | Очередь обновления статей: сроки пересмотра плюс динамика спроса | команда |
| `wordstat/discover.mjs` | Недельный опрос Wordstat по 212 сидам, до 2000 фраз на сид | workflow |
| `wordstat/fetch.mjs` | Частотность по конкретным ключам плюс динамика | workflow |
| `wordstat/extract-keys.mjs` | Собирает ключи-кандидаты из блога и контент-плана | workflow |
| `wordstat/diff-snapshots.mjs` | Сравнение выгрузок неделя к неделе: новые, растущие, падающие, исчезнувшие | workflow |
| `wordstat/enrich-candidates.mjs` | Дотягивает недельную динамику для кандидатов | workflow |
| `wordstat/demand-watch.mjs` | Спрос по всем кластерам блога одним отчётом | workflow, команда |
| `wordstat/relevance.mjs` | Отбор фраз, годных как тема: навигационный шум и омонимы | `generate-backlog`, `enrich-candidates` |
| `wordstat/cleanup-orphans.mjs` | Чистит из кеша фразы, которых больше нет в источниках | workflow |
| `wordstat/prune-history.mjs` | Ретенция дампов: не даёт репозиторию расти бесконечно | workflow |

### Проверки текста

| Скрипт | Что делает | Кто запускает |
|---|---|---|
| `check-seo.mjs` | SEO одной статьи: блокирующие требования и предупреждения | хук, выпуск, команда |
| `audit/check-seo-all.mjs` | То же по всему корпусу, падает на любом замечании | CI |
| `check-ai-markers.mjs` | Канцелярит и машинные штампы, скор 0–10 | выпуск, команда |
| `audit/check-blog-links.mjs` | Битые внутренние ссылки | CI, выпуск |
| `audit/fix-broken-blog-links.mjs` | Их же автопочинка | вручную |
| `audit/check-market-duplication.mjs` | Пересечение темы с каталогом Контур.Маркета | `generate-backlog`, команда |
| `audit/linkgraph.mjs` | Граф перелинковки: сироты, тупики, кандидаты на ссылку | команда |
| `audit/set-review-dates.mjs` | Проставляет дату пересмотра там, где её нет | вручную |
| `check-stale-content.mjs` | Статьи, которые пора пересмотреть по сроку | workflow |
| `factcheck/check-report.mjs` | Гейт на доказательство факта: сформулировано ли утверждение, есть ли дословная цитата первоисточника с этим значением, согласована ли уверенность со статусом. Без него `write-marker` не выписывает маркер | `/factcheck`, гейт статьи, выпуск |
| `check-analysis.mjs` | Гейт на оценщика: замечание стоит балла, снятый балл объяснён, бонусов нет, порог 85 | `/analyze-article`, выпуск |
| `gates.mjs` | Единый гейт. `<slug>` — 12 проверок написанной статьи; `--topic "<тема>"` — гейт до работы (дубли и каталог Маркета одним вызовом); `--audit` — распределение ответов по корпусу. Три исхода: зелёное / красное / требует решения. `--json` отдаёт блок `checks` для `/analyze-article` | `npm run gates`, стадии 1 и 3 пайплайна |
| `check-update-doc.mjs` | Форма документа актуализации: цитаты «Было», источники у правок, раздел «что оставить», подводки у промо | команда `/update-article` |

### Факты и нормативка

| Скрипт | Что делает | Кто запускает |
|---|---|---|
| `factcheck/extract-claims.mjs` | Достаёт из текста проверяемые утверждения девяти типов | команда `/factcheck` |
| `factcheck/write-marker.mjs` | Ставит отметку о проверке с хешем текста. Без отчёта отказывается | команда `/factcheck` |
| `factcheck/audit-marker-hashes.mjs` | Отметка соответствует закоммиченному тексту | CI |
| `factcheck/audit-marker-results.mjs` | У каждой выпущенной статьи факчек доведён до отчёта | CI |
| `factcheck/audit-npa-references.mjs` | Номера законов существуют и относятся к теме статьи | CI, выпуск |
| `npa-change-candidates.mjs` | Фактура для квартального отчёта «где поменялась нормативка» | команда |

### Общее и служебное

| Скрипт | Что делает | Кто запускает |
|---|---|---|
| `console.mjs` | Точка входа: панель состояния и меню запуска остального | вручную |
| `health-check.mjs` | Единый отчёт состояния модуля и бизнес-инвариантов | workflow, вручную |
| `lib/sheet-columns.mjs` | Колонки таблицы редакции — единственный источник правды | `cycle-state`, `drive-sync`, `verify-sheet` |
| `lib/slugify.mjs` | Транслитерация заголовка в slug, одна на весь проект | `cycle-state`, `drive-sync` |
| `lib/text-similarity.mjs` | Сравнение тем по словам | `generate-backlog`, дедуп с Маркетом |
| `lib/resolve-links.mjs` | Внутренние ссылки статьи → кликабельные адреса | `drive-sync` |
| `lib/write-report.mjs` | Пишет отчёт аудита, только если он реально изменился | `linkgraph`, `audit-npa-references` |
| `git-hooks/pre-commit-factcheck-guard.mjs` | Не даёт закоммитить выпущенную статью без пройденного факчека | git-хук |
| `git-hooks/pre-commit-seo-guard.mjs` | То же для блокирующих SEO-ошибок | git-хук |

Не помнить всё это наизусть — `npm run console`: панель состояния и
пронумерованное меню. Добавили или переименовали скрипт — впишите пункт
в `MENU` внутри `console.mjs` и прогоните `node scripts/console.mjs check`:
он ловит пункты, указывающие в никуда, и задвоенные номера.

Ключи для скриптов с сетевыми вызовами лежат в секретах GitHub, локально
не прописаны — см. `docs/SECRETS.md`.

### Где скрипт сужает, а решает ИИ

Шесть мест, где детерминированный проход оставляет решение сессии.
Устроены одинаково: скрипт делает дешёвую часть и явно показывает, что
осталось; сессия отвечает на вопрос, который счётчиком не берётся;
результат возвращается в файл и дальше живёт как обычные данные.

| Место | Что делает скрипт | Что решает сессия |
|---|---|---|
| Извлечение утверждений | находит классы, которые перечислены: даты, суммы, номера норм | читает статью и выписывает остальное — длительности, кратности, доли; вливается командой `extract-claims merge` |
| Похожесть на снятую тему | глушит от 0.4, пропускает ниже 0.25 | разбирает пограничные 0.25–0.4: та же тема или другая |
| Машинный текст | считает стоп-фразы и ставит порог 6 из 10 | отвечает на то, чего список не видит: одинаковая форма абзацев, оговорки без содержания, симметрия ради симметрии |
| Журнал правок редакции | фиксирует «было/стало» по абзацам и заголовкам | называет закономерность строкой `**Вывод:**`; повторилась трижды — правило в редполитику |
| Актуализация чужой статьи | разбирает задачу на поля, строит очередь по трафику, проверяет форму документа правок | читает исходную статью и решает, что в ней устарело, что оставить и чем подтвердить |
| Оценка статьи | гоняет 8 проверок как pass/fail и проверяет арифметику самой оценки: замечание стоило балла, снятый балл объяснён, бонусов нет | читает текст и ставит баллы за лид, структуру, язык и пользу — то, что видно только при чтении |

Граница везде одна: **гейт остаётся за скриптом**. Порог, код возврата,
хеш маркера, запись в состояние — детерминированные. Проверка, чей
вердикт меняется от прочтения, гейтом быть не может.

### Переопределение путей для тестов

У скриптов, которые читают репозиторий, есть переменная окружения для
подмены корня: `CYCLE_STATE_PATH`, `SUPPRESSIONS_PATH`, `HEALTH_CHECK_ROOT`,
`UPDATE_QUEUE_ROOT`, `UPDATE_DOC_ROOT`, `ANALYZE_ROOT`, `GATES_ROOT`, `MARKET_ROOT`, `LINKGRAPH_ROOT`,
`FACTCHECK_ROOT`, `EDITS_ROOT`, `NPA_ROOT`, `DIGEST_ROOT`, `STALE_ROOT`,
`SEO_AUDIT_ROOT`, `AI_PROFILE_ROOT`. Тесты гоняют скрипт на временной
фикстуре, а не на живом корпусе: иначе проверка «статья устарела» зависит
от того, какой сегодня день, а «тема заглушена» — от того, что редакция
сняла на этой неделе.

`check-blog-links` и `audit-npa-references` работают от текущего каталога,
им переменная не нужна — тест запускает их с `cwd` фикстуры.

Новый скрипт, который читает файлы репозитория, заводит такую же
переменную сразу. Скрипт, который нельзя запустить на фикстуре, нельзя и
проверить.

### Правило для новых скриптов

Скрипт, который читают как проверку, не должен менять рабочее дерево.
Отчёты пишутся через `lib/write-report.mjs`: он сравнивает содержимое без
отметки времени и не трогает файл, если ничего не изменилось. Иначе
любой запуск аудита делает дерево грязным, и следующий, кто его запустит
— человек или агент, — тратит время на вопрос «а это я сломал?».

## Документация изменений

При изменении скрипта, агента или паттерна — **обновлять соответствующий
файл в `docs/` в той же PR**. Не накапливать долг.

| Что меняется | Какой doc обновить |
|---|---|
| Редакционный цикл, рутины, машина состояний | `docs/editorial-cycle.md` |
| Доступ к Google API, скоупы, токены | `docs/google-api-setup.md` |
| Wordstat API, частотность ключей | `docs/wordstat.md` |
| Factcheck pipeline, `extract-claims`, `sources.json`, аудит НПА | `docs/factcheck.md` |
| Backfill истории и системные паттерны факт-чека | `docs/factcheck-history.md` |
| CI, юнит-тесты | `docs/operations.md` (раздел «CI») |
| Редполитика, классы решений | `docs/editorial-policy.md` |
| Правила контента и стиля, frontmatter | `docs/content-rules.md` |
| Как добавить статью, термин, pillar | `docs/content-types.md` |
| Секреты GitHub | `docs/SECRETS.md` |
| Ритуалы и «что делать когда горит» | `docs/operations.md` |
| Post-mortem крупных сессий | `docs/archive/sessions/session-YYYY-MM-DD-postmortem.md` |
| Этот файл | `docs/tools.md` |

Если изменение не покрыто существующим документом — расширить ближайший
по теме или завести новый и добавить ссылку в `AGENTS.md`.

## Каталог CPA-блоков

`src/data/cpa-blocks.json` — блоки Контура, которые Стадия 7
`/create-article` предлагает под подводки в статье. Не редактируется
руками: пересобирается из выгрузки админки.

```bash
python3 scripts/import-cpa-blocks.py <выгрузка.xlsx>
python3 scripts/import-cpa-blocks.py <выгрузка.xlsx> --dry-run
```

Скрипт чистит описания от HTML, схлопывает дубли по тексту и посадочной
и проставляет `cluster` по адресу ссылки — по нему агент и подбирает
блок под кластер статьи. Блоки без посадочной отбрасываются: вести
читателя некуда. Блоки без заголовка остаются — у трети выгрузки это
голая кнопка со ссылкой, и она рабочая.

Требует `openpyxl` (`pip install openpyxl`). Гоняется по мере
поступления свежих выгрузок, в пайплайне не участвует.

### Проверка промоблоков в check-seo.mjs

`check-seo.mjs` разбирает маркеры `[Промоблок: <id>]` в теле статьи и
сверяет их с `src/data/cpa-blocks.json`:

- **P0** — `id` нет в каталоге. Выдуманный номер ведёт в никуда так же,
  как выдуманный номер постановления, а на глаз это не отличить.
- **P1** — блок из другого кластера, чем статья; один и тот же блок
  повторяется; подводок не 3.

Статья без промоблоков проверку не вызывает — она не обязательна для
материалов, под которые нет оффера.
