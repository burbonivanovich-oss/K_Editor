# Рутина A — контент-план цикла

- **Триггер:** `trig_01UK3gQ3ZaJ1SqcUnddxmxWR`
- **Расписание:** `0 8 1 * *`
- **Окружение:** Editor
- **Репозиторий:** `burbonivanovich-oss/K_Editor`

---

Ты работаешь в репозитории burbonivanovich-oss/K_Editor — модуль-помощник для редактора и копирайтеров по тематике МСБ РФ (ТС ПИоТ, маркировка Честный знак, изменения в законодательстве). Инструкции проекта — CLAUDE.md, AGENTS.md и docs/, прочитай перед началом.

Это рутина A редакционного цикла. Выполни процедуру из .claude/commands/cycle-plan.md целиком, по шагам, ничего не пропуская. Полное описание цикла — docs/editorial-cycle.md.

Порядок работы:

1. Синхронизируй рабочую копию: git fetch origin main && git checkout main && git pull origin main.

2. Шаг 0 процедуры — предохранитель. node scripts/cycle-state.mjs get. Состояние НЕ idle и НЕ done — СТОП: выведи ссылку на таблицу текущего цикла и завершись. Две таблицы плана одновременно — гарантия путаницы у редактора.

3. Дальше по cycle-plan.md: сбор тем (шаг 1), временный файл плана в scratchpad (шаг 2), init-cycle в Drive (шаг 3), фиксация состояния и коммит (шаг 4), сообщение редактору (шаг 5).

Что критично не нарушить:

- Контент-план src/content/wiki/content-plan-2026.md на этой стадии НЕ трогать. План живёт в таблице и в состоянии цикла; в репозиторий он попадёт только после согласования, это делает рутина B. После прогона проверь git status: контент-план должен быть чист.
- Сразу после init-cycle сделай push-plan со slug'ами. init-cycle пишет в скрытую колонку K «ID (не менять)» значение поля slug из файла плана, а cycle-plan.md запрещает указывать slug в этом файле — колонка остаётся пустой, и первый проход рутины B сопоставляет строки по номеру строки вместо стабильного id. Поэтому: после cycle-state.mjs init собери из src/data/editorial-cycle.json файл плана с полями title, priority, cluster, targetKeyword, rationale, slug (status НЕ включать) и прогони node scripts/drive-sync.mjs push-plan --cycle <id> --sheet-id <id> --plan <файл>.
- Изменение src/data/editorial-cycle.json обязано попасть в репозиторий: закоммить и запушь, следующая рутина стартует в другой сессии и читает состояние оттуда.
- Не запускай /create-article — это работа рутины C.
- Не больше 8 P0 на цикл, язык таблицы только русский.

Проверка перед завершением: node scripts/cycle-state.mjs get показывает awaiting_review и нужное число тем; node scripts/health-check.mjs — 0 fail.

В конце — короткий отчёт по форме из cycle-plan.md: цикл, число тем с разбивкой по приоритетам, ссылки на таблицу и папку, состояние.
