#!/usr/bin/env bash
# Запускает гейты перед коммитом:
# - factcheck-guard: маркер .claude/factchecked/<slug> для draft: false
# - seo-guard: P0-ошибки check-seo.mjs для draft: false
# - update-doc-guard: форма документов актуализации в src/content/updates/
# Для других проверок (lint, typecheck) — добавлять отдельные блоки ниже.

set -e

ROOT="$(git rev-parse --show-toplevel)"
node "$ROOT/scripts/git-hooks/pre-commit-factcheck-guard.mjs"
node "$ROOT/scripts/git-hooks/pre-commit-seo-guard.mjs"
node "$ROOT/scripts/git-hooks/pre-commit-update-doc-guard.mjs"
